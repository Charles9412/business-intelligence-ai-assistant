services:
  app:
    image: python:3.11-slim
    working_dir: /workspace
    volumes:
      - .:/workspace
    env_file:
      - .env
    command: python -m app.main
    # TODO: Replace this placeholder service with a production-ready local dev command.
